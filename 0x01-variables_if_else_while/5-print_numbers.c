#include <stdio.h>
/**
 * main - decribes the function
 *
 * Return: returns 0 when the code is correct
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
