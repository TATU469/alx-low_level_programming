#include <stdio.h>
/**
 * main - describes the function
 *
 * Return: returns 0 when code is correct
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		putchar(i + '0');
	putchar('\n');
	return (0);
}
