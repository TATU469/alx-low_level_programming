#include <stdio.h>
/**
 * main - describes the function
 *
 * Return: returns 0 when the code is correct
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
